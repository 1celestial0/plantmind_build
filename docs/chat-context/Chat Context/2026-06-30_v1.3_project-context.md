# PlantMind — Project Context File
**Version:** 1.3 | **Date:** 2026-06-30 | **Changes from v1.2:** Wave 1 ops + lanes + contracts + research 3–6 + horizon scaffolds; demo deferred
**Rule:** Read latest version at session start.

---

## 1. Project snapshot

PlantMind hackathon build (2026-07-09). **Spine complete:** ops manuals, lane prompts, `src/contracts/`, research Phases 3–6 locked. **Demo deferred:** v1 legacy has known bugs (RUL snapshot, FORGE imports); user chose to skip deep v1 testing and build v2 lanes instead.

---

## 2. What's new since v1.2

| # | What |
|---|---|
| + | `continuity/goal-log.json` + `ops/OPERATIONS-MANUAL.md` + `ops/TEAM-CHAT-GUIDE.md` |
| + | `ops/prompts/lanes/lane-01..05.md` |
| + | `src/contracts/` Pydantic models (LOCKED_STATE §4) |
| + | Research: DATABRICKS_MAP, DATA_REALITY, ROI_BENCHMARKS, ARCHITECTURE_LOCK (2026-06-30) |
| + | `scripts/export-to-drive.ps1`, `scripts/sync-notion-drive.ps1` |
| + | `ml/tracking/mlflow_config.py`, `ops/skills/plantmind-session/` |
| ~ | ROADMAP NOW → Lane 1/2 build; demo items → DEFERRED |

---

## 3. Current architecture / approach

Layer 0 contracts in `src/contracts/`. Next build wave per `ARCHITECTURE_LOCK_2026_30th_June.md`: P1 physics (L2) → P2 agents (L1) → P3 dashboard (L3). Truth: LOCKED_STATE > ARCHITECTURE_LOCK > ROADMAP > this file.

---

## 4. Key files

| File | Role |
|---|---|
| `docs/research/ARCHITECTURE_LOCK_2026_30th_June.md` | Build sequence lock |
| `src/contracts/` | Inter-lane API |
| `ops/OPERATIONS-MANUAL.md` | Team operating manual |
| `ops/prompts/lanes/` | One prompt per parallel chat |
| `continuity/goal-log.json` | Goal completion history |

---

## 5. Open questions

- GitHub org + publish timing
- 6th agent: MaintenanceScheduler vs SafetyGuardian
- When to un-defer v1 demo fixes (before T-6h freeze)

---

## 6. Risks / known issues

- v1 Streamlit predicts at end-of-life → RUL displays as 0 (design bug)
- `FORGE` import paths in legacy code — junction workaround only
- Git remote not configured

---

## 7. External dependencies

| Dependency | Status |
|---|---|
| Git remote | Not configured |
| Drive / Notion | IDs in STATE.json; export script ready, sync manual |

---

## 8. Verbatim user constraints

> Skip working demo sections now — code not written correctly yet; fulfill rest of goals with priorities.
> Proceed with all Goals — demo smoke deferred.