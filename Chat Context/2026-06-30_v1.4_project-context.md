# PlantMind — Project Context File
**Version:** 1.4 | **Date:** 2026-06-30 | **Changes from v1.3:** Session close — Team Playbook, CLI parity, testing framework, parallel docs
**Rule:** Read latest version at session start.

---

## 1. Project snapshot

Wave 1 spine complete. **Tomorrow:** Lane 2 physics + Lane 1 agents (ROADMAP NOW). v1 demo deferred. All CLIs must follow `ops/CLI-PARITY.md` and `ops/TEAM-OPERATIONS-PLAYBOOK.md`.

---

## 2. What's new since v1.3

| # | What |
|---|---|
| + | `ops/TEAM-OPERATIONS-PLAYBOOK.md` — canonical start/close/status manual |
| + | `ops/CLI-PARITY.md` — identical AI behavior all tools |
| + | `ops/testing/scenarios.json` + generator + `SCENARIO-CATALOG.md` |
| + | `tests/test_contracts.py`, `tests/test_scenarios.py` (14 passing) |
| + | `continuity/test-log.json` + `scripts/run-tests.ps1` |
| + | `scripts/check-status.ps1` |
| + | `docs/parallel/` — lane living docs + STATUS rollup |

---

## 3. Resume tomorrow

```powershell
cd "C:\Users\hp\Claude\Projects\PlantMind\PlantMind_live"
powershell -ExecutionPolicy Bypass -File .\scripts\start-session.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\check-status.ps1
```

First AI message: read playbook + summarize ROADMAP NOW → propose Lane 2 goals.

---

## 4. Open NOW (unchanged)

- Lane 2: `src/physics/` + `ml/synthesis/`
- Lane 1: agent stubs + orchestrator + FastAPI

---

## 5. Session close state

- Tests: 14 passed, logged to `continuity/test-log.json`
- Git: committed session-close batch
- Demo v1: still DEFERRED

---

## 6. Verbatim user constraints

> Wrap session; resume tomorrow; same CLI behavior; operational manual; automatic testing scenarios + test logs; parallel documentation.