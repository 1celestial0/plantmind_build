# PlantMind — Project Context File
**Version:** 1.6 | **Date:** 2026-06-30 | **Changes from v1.5:** Lanes 2, 1, and 3 all built and browser-verified in single session.

---

## 1. Project snapshot

All three core lanes are complete and end-to-end verified. The demo pipeline is runnable today.

```
streamlit run src/dashboard/app.py      # full UI
uvicorn src.api.main:app --reload       # REST API
python -m scripts.smoke_test            # CLI hero scenario
python -m pytest tests/ -q             # 78 tests passing
```

**Hackathon:** 2026-07-09 — 9 days out.

---

## 2. What's built (this session)

### Lane 2 — `src/physics/` + `ml/synthesis/`
| File | Purpose |
|---|---|
| `src/physics/constants.py` | LOCKED_STATE §6a Weibull params (λ, β per type) |
| `src/physics/stress.py` | Arrhenius AF_temp + power-law AF_load → S |
| `src/physics/weibull.py` | H(t), t_fail solver, 95% CI via bootstrap |
| `src/physics/model.py` | `compute_health()` public API → PhysicsModelOutput |
| `ml/synthesis/config.py` | 20 signal specs, 3 failure modes, op-condition ranges |
| `ml/synthesis/generator.py` | 30 assets × 500 cycles → `ml/data/synthetic/` (15k rows) |
| `ml/synthesis/calibrate_weibull.py` | MLE λ calibration + chi-sq CI (ready for CMAPSS) |

### Lane 1 — `src/agents/` + `src/pipeline/` + `src/api/` + `src/governance/`
| File | Purpose |
|---|---|
| `src/agents/data_sentinel.py` | Z-score + Mahalanobis → normal/warning/critical |
| `src/agents/asset_health_oracle.py` | Wraps physics → AssetHealthReport; trigger check |
| `src/agents/gotze_engine.py` | IIS scoring, 8 candidates, safety veto, urgency nudge |
| `src/agents/root_cause_analyst.py` | Template RCA + RAG citations (stub; swap Groq later) |
| `src/agents/executive_summarizer.py` | 3-field ExecutiveBrief (stub; swap Groq later) |
| `src/pipeline/orchestrator.py` | 5-agent chain → AuditRecord write → PipelineResult |
| `src/governance/audit.py` | SQLite audit store: write/get/list/update_decision |
| `src/rag/store.py` | ChromaDB stub with final interface |
| `src/api/main.py` | FastAPI app: `/analyze`, `/asset/{id}/health`, `/decisions`, `/audit` |

### Lane 3 — `src/dashboard/`
| File | Purpose |
|---|---|
| `app.py` | st.navigation() entry point, session state init |
| `demo_scenarios.py` | 5 pre-wired SensorReadings (A–E) |
| `pages/plant_overview.py` | Health gauge + RUL bar + anomaly badges + exec metrics |
| `pages/gotze_decision.py` | IIS bar chart + formula breakdown + Approve/Reject gate |
| `pages/audit_log.py` | Colour-coded table + lineage detail viewer |

---

## 3. Demo flow (verified in browser)

1. `streamlit run src/dashboard/app.py`
2. Sidebar: select **Scenario A** → click **Run Analysis**
3. Plant Overview: health 18.8/100, CRITICAL, 7 anomaly badges, $180k saved
4. Navigate to **GötzeEngine Decision**: PREDICTIVE SHUTDOWN (IIS 0.868), full bar chart
5. Click **Approve** → green confirmation + audit record updated
6. Navigate to **Audit Log**: `approved` in green at top, lineage 5 steps

Pipeline latency: ~18–22 ms end-to-end.

---

## 4. Remaining NEXT items (from ROADMAP)

| Priority | Item |
|---|---|
| HIGH | Lane 5: judge Q&A bank + ROI calculator (`ops/runbooks/`) |
| HIGH | Lane 4: Databricks runbook + notebook skeleton (`deploy/databricks/`) |
| MED | Wire scenario injector A–E to `ml/synthesis/` integration pytest |
| MED | Groq LLM wiring for RootCauseAnalyst + ExecutiveSummarizer |
| LOW | 6th agent decision: MaintenanceScheduler vs SafetyGuardian |

---

## 5. Run commands (quick ref)

```powershell
# Tests
python -m pytest tests/ -q

# Dashboard (main demo)
python -m streamlit run src/dashboard/app.py

# API
uvicorn src.api.main:app --reload

# Smoke test (CLI)
python -m scripts.smoke_test

# Data generator
python -m ml.synthesis.generator

# Weibull calibration (when failure data available)
python -m ml.synthesis.calibrate_weibull --asset-type pump --failure-cycles 410 425 --beta 2.3
```

---

## 6. Test count

| Suite | Tests | Status |
|---|---|---|
| test_contracts | 5 | pass |
| test_physics | 32 | pass |
| test_agents | 22 | pass |
| test_pipeline | 10 | pass |
| test_scenarios | 9 | pass |
| **Total** | **78** | **all pass** |
